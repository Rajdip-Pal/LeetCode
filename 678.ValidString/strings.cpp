#include<iostream>
#include<stack>

bool isValid(const std::string example){
    std::stack<char> symbols;
}

int main() {
    std::string userInput;
    std::getline(std::cin,userInput);
    std::cout << std::boolalpha << isValid(userInput) << std::endl;
    return 0;
}